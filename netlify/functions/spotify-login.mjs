import crypto from 'node:crypto';
import {cookie,methodAllowed,safeOrigin} from './_security.mjs';
export default async request=>{if(!methodAllowed(request,['GET']))return new Response(null,{status:405,headers:{Allow:'GET'}});try{const clientId=process.env.SPOTIFY_CLIENT_ID;if(!clientId)throw new Error('configuration');const state=crypto.randomBytes(24).toString('hex'),verifier=crypto.randomBytes(64).toString('base64url'),challenge=crypto.createHash('sha256').update(verifier).digest('base64url'),origin=safeOrigin(request),redirect=process.env.SPOTIFY_REDIRECT_URI||`${origin}/.netlify/functions/spotify-callback`,query=new URLSearchParams({client_id:clientId,response_type:'code',redirect_uri:redirect,scope:'user-read-private',state,code_challenge_method:'S256',code_challenge:challenge});return new Response(null,{status:302,headers:{Location:`https://accounts.spotify.com/authorize?${query}`,'Set-Cookie':cookie('dunder_oauth',`${state}.${verifier}`,600),'Cache-Control':'no-store'}})}catch{return new Response(null,{status:302,headers:{Location:'/?spotify=unavailable','Cache-Control':'no-store'}})}};
